#include <bits/stdc++.h>
using namespace std;

int main(){
multimap<pair<int,int> m; //to store multiple value coreesponding any key rest every thing is same as map
//or we can use map as below to store multiple value coreesponding any key
//map<int,vector<int>> m;

//DIFFERENCE between map and multimap is that wehen we insert any other value regarding any key then map update that word but when when we use multimap it add that value to that key
    return 0;
}