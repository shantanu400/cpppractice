#include <bits/stdc++.h>
using namespace std;

int main(){

map<set<int>,int> m;
set<int> s1={1,2,3};  //firstly s1.first will compare with s2.first and if found s1.first greater the s1 will be store in map first as map store data after shorting
set<int> s2={2,3};

    return 0;
}